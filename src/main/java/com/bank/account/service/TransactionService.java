package com.bank.account.service;

import com.bank.account.model.Account;

public class TransactionService {

    //checks whether the resultant amount is greater or equal to 0
    public boolean ifValidTransaction(Account account) {
        return false;
    }

    //checks whether the given amount to be added is a valid integer or not
    public boolean ifValidAmount(int amount){
        return false;
    }
}
